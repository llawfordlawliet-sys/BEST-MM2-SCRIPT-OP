-- ---- YAGAMI HUB - MURDER MYSTERY 2 ULTIMATE
-- FULLY WORKING: Aimbot | Silent Aim | Fling | Auto Notifications | Auto Farm Coins | ESP (0.1s Update)
-- Works on all executors (Delta, Synapse, Krnl, ScriptWare, Fluxus, Oxygen U)

repeat task.wait() until game:IsLoaded()

local plr = game:GetService("Players").LocalPlayer
local UserInput = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local Players = game:GetService("Players")

-- Create GUI
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "YagamiHub"
screenGui.Parent = plr:WaitForChild("PlayerGui")
screenGui.ResetOnSpawn = false
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local retroFont = Enum.Font.GothamBold
local modernFont = Enum.Font.Gotham

-- ============================================
-- VARIABLES
-- ============================================
local currentRole = "Innocent"
local aimbotEnabled = false
local silentAimEnabled = false
local showFOVCircle = false
local autoCollectEnabled = false
local espEnabled = false
local espObjects = {}
local fovCircle = nil
local fovRadius = 200
local circleAnimation = nil
local lastNotificationTime = 0
local currentMurderer = nil
local currentSheriff = nil
local roundActive = false
local lastESPUpdate = 0

-- ROLE COLORS
local ROLE_COLORS = {
    Murderer = Color3.fromRGB(255, 40, 40),
    Sheriff = Color3.fromRGB(40, 120, 255),
    Innocent = Color3.fromRGB(40, 255, 40)
}

-- ============================================
-- INVENTORY SCANNER (ACCURATE ROLE DETECTION)
-- ============================================
local function scanInventoryForRole(player)
    local backpack = player:FindFirstChild("Backpack")
    if backpack then
        for _, item in pairs(backpack:GetChildren()) do
            if item:IsA("Tool") then
                if item.Name == "Gun" then
                    return "Sheriff"
                elseif item.Name == "Knife" then
                    return "Murderer"
                end
            end
        end
    end
    
    local character = player.Character
    if character then
        for _, item in pairs(character:GetChildren()) do
            if item:IsA("Tool") then
                if item.Name == "Gun" then
                    return "Sheriff"
                elseif item.Name == "Knife" then
                    return "Murderer"
                end
            end
        end
    end
    
    return "Innocent"
end

local function getPlayerRole(player)
    if player == plr then return currentRole end
    return scanInventoryForRole(player)
end

local function updateRoleVariables()
    currentMurderer = nil
    currentSheriff = nil
    
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= plr then
            local role = scanInventoryForRole(player)
            if role == "Murderer" then
                currentMurderer = player
            elseif role == "Sheriff" then
                currentSheriff = player
            end
        end
    end
end

local function updateSelfRole()
    if plr.Character then
        for _, v in pairs(plr.Character:GetChildren()) do
            if v:IsA("Tool") then
                if v.Name == "Gun" then
                    currentRole = "Sheriff"
                    return
                elseif v.Name == "Knife" then
                    currentRole = "Murderer"
                    return
                end
            end
        end
    end
    
    local backpack = plr:FindFirstChild("Backpack")
    if backpack then
        for _, item in pairs(backpack:GetChildren()) do
            if item:IsA("Tool") then
                if item.Name == "Gun" then
                    currentRole = "Sheriff"
                    return
                elseif item.Name == "Knife" then
                    currentRole = "Murderer"
                    return
                end
            end
        end
    end
    
    currentRole = "Innocent"
end

-- ============================================
-- AIMBOT (LOCKS ONTO MURDERER'S TORSO)
-- ============================================
local aimbotConnection = nil

local function getMurderer()
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= plr and player.Character and getPlayerRole(player) == "Murderer" then
            return player
        end
    end
    return nil
end

local function getSheriff()
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= plr and player.Character and getPlayerRole(player) == "Sheriff" then
            return player
        end
    end
    return nil
end

local function updateAimbot()
    if not aimbotEnabled then return end
    
    local hasGun = plr.Character and plr.Character:FindFirstChild("Gun")
    if not hasGun then return end
    
    local murderer = getMurderer()
    if not murderer or not murderer.Character then return end
    
    local targetPart = murderer.Character:FindFirstChild("HumanoidRootPart")
    if not targetPart then
        targetPart = murderer.Character:FindFirstChild("UpperTorso")
    end
    if not targetPart then return end
    
    local camera = Workspace.CurrentCamera
    local vector, onScreen = camera:WorldToScreenPoint(targetPart.Position)
    
    if onScreen then
        local mouse = plr:GetMouse()
        local deltaX = vector.X - mouse.X
        local deltaY = vector.Y - mouse.Y
        
        -- Smooth aimbot
        local smoothFactor = 0.25
        mousemoverel(deltaX * smoothFactor, deltaY * smoothFactor)
    end
end

local function startAimbot()
    if aimbotConnection then aimbotConnection:Disconnect() end
    aimbotConnection = RunService.RenderStepped:Connect(updateAimbot)
end

local function stopAimbot()
    if aimbotConnection then
        aimbotConnection:Disconnect()
        aimbotConnection = nil
    end
end

-- ============================================
-- SILENT AIM (WORKS WITH ANY WEAPON)
-- ============================================
local silentAimConnection = nil

local function getClosestPlayer()
    local character = plr.Character
    if not character then return nil end
    
    local rootPart = character:FindFirstChild("HumanoidRootPart")
    if not rootPart then return nil end
    
    local closest = nil
    local closestDist = math.huge
    
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= plr and player.Character then
            local targetRoot = player.Character:FindFirstChild("HumanoidRootPart")
            if targetRoot then
                local dist = (targetRoot.Position - rootPart.Position).Magnitude
                if dist < closestDist then
                    closestDist = dist
                    closest = player
                end
            end
        end
    end
    
    return closest
end

local function updateSilentAim()
    if not silentAimEnabled then return end
    
    local tool = plr.Character and plr.Character:FindFirstChildWhichIsA("Tool")
    if not tool then return end
    
    local targetPlayer = getClosestPlayer()
    if not targetPlayer or not targetPlayer.Character then return end
    
    local targetHead = targetPlayer.Character:FindFirstChild("Head")
    if not targetHead then return end
    
    local camera = Workspace.CurrentCamera
    local vector, onScreen = camera:WorldToScreenPoint(targetHead.Position)
    
    if onScreen then
        local mouse = plr:GetMouse()
        mousemoverel(vector.X - mouse.X, vector.Y - mouse.Y)
        
        if tool:FindFirstChild("Fire") then
            local args = {mouse.Hit}
            pcall(function()
                tool.Fire:FireServer(unpack(args))
            end)
        end
    end
end

local function startSilentAim()
    if silentAimConnection then silentAimConnection:Disconnect() end
    silentAimConnection = RunService.RenderStepped:Connect(updateSilentAim)
end

local function stopSilentAim()
    if silentAimConnection then
        silentAimConnection:Disconnect()
        silentAimConnection = nil
    end
end

-- ============================================
-- FLING FUNCTION (MURDERER OR SHERIFF)
-- ============================================
local function flingPlayer(targetPlayer)
    if not targetPlayer or not targetPlayer.Character then return false end
    
    local targetRoot = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not targetRoot then return false end
    
    local originalPos = targetRoot.Position
    
    -- Create powerful velocity
    local bodyVelocity = Instance.new("BodyVelocity")
    bodyVelocity.Velocity = Vector3.new(math.random(-200, 200), 300, math.random(-200, 200))
    bodyVelocity.MaxForce = Vector3.new(1e6, 1e6, 1e6)
    bodyVelocity.Parent = targetRoot
    
    -- Also add BodyGyro to prevent them from controlling flight
    local bodyGyro = Instance.new("BodyGyro")
    bodyGyro.MaxTorque = Vector3.new(1e6, 1e6, 1e6)
    bodyGyro.Parent = targetRoot
    
    task.wait(0.6)
    
    -- Teleport back to original position
    targetRoot.CFrame = CFrame.new(originalPos)
    
    -- Clean up
    bodyVelocity:Destroy()
    bodyGyro:Destroy()
    
    return true
end

-- ============================================
-- AUTO ROUND NOTIFICATION (RIGHT SIDE)
-- ============================================
local function createNotification(title, message, color)
    local notifContainer = Instance.new("Frame")
    notifContainer.Size = UDim2.new(0, 320, 0, 0)
    notifContainer.Position = UDim2.new(1, -340, 1, -190)
    notifContainer.BackgroundTransparency = 1
    notifContainer.ClipsDescendants = true
    notifContainer.Parent = screenGui
    
    local notifFrame = Instance.new("Frame")
    notifFrame.Size = UDim2.new(1, 0, 0, 0)
    notifFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
    notifFrame.BackgroundTransparency = 0.1
    notifFrame.BorderSizePixel = 0
    notifFrame.Parent = notifContainer
    
    local notifCorner = Instance.new("UICorner")
    notifCorner.CornerRadius = UDim.new(0, 12)
    notifCorner.Parent = notifFrame
    
    local glowFrame = Instance.new("Frame")
    glowFrame.Size = UDim2.new(1, 0, 1, 0)
    glowFrame.BackgroundColor3 = color
    glowFrame.BackgroundTransparency = 0.9
    glowFrame.BorderSizePixel = 0
    glowFrame.Parent = notifFrame
    
    local glowCorner = Instance.new("UICorner")
    glowCorner.CornerRadius = UDim.new(0, 12)
    glowCorner.Parent = glowFrame
    
    local accentBar = Instance.new("Frame")
    accentBar.Size = UDim2.new(0, 4, 1, 0)
    accentBar.BackgroundColor3 = color
    accentBar.BorderSizePixel = 0
    accentBar.Parent = notifFrame
    
    local icon = Instance.new("TextLabel")
    icon.Size = UDim2.new(0, 35, 0, 35)
    icon.Position = UDim2.new(0, 12, 0, 12)
    icon.BackgroundTransparency = 1
    icon.Text = title == "MURDERER" and "🔪" or "🔫"
    icon.TextColor3 = color
    icon.TextSize = 24
    icon.Font = retroFont
    icon.TextXAlignment = Enum.TextXAlignment.Center
    icon.TextYAlignment = Enum.TextYAlignment.Center
    icon.Parent = notifFrame
    
    local titleLabel = Instance.new("TextLabel")
    titleLabel.Size = UDim2.new(1, -70, 0, 22)
    titleLabel.Position = UDim2.new(0, 55, 0, 10)
    titleLabel.BackgroundTransparency = 1
    titleLabel.Text = title
    titleLabel.TextColor3 = color
    titleLabel.TextSize = 16
    titleLabel.Font = retroFont
    titleLabel.TextXAlignment = Enum.TextXAlignment.Left
    titleLabel.Parent = notifFrame
    
    local msgLabel = Instance.new("TextLabel")
    msgLabel.Size = UDim2.new(1, -70, 0, 22)
    msgLabel.Position = UDim2.new(0, 55, 0, 32)
    msgLabel.BackgroundTransparency = 1
    msgLabel.Text = message
    msgLabel.TextColor3 = Color3.fromRGB(200, 200, 220)
    msgLabel.TextSize = 13
    msgLabel.Font = modernFont
    msgLabel.TextXAlignment = Enum.TextXAlignment.Left
    msgLabel.Parent = notifFrame
    
    local closeBtn = Instance.new("TextButton")
    closeBtn.Size = UDim2.new(0, 22, 0, 22)
    closeBtn.Position = UDim2.new(1, -30, 0, 8)
    closeBtn.BackgroundTransparency = 1
    closeBtn.Text = "✕"
    closeBtn.TextColor3 = Color3.fromRGB(150, 150, 180)
    closeBtn.TextSize = 14
    closeBtn.Font = retroFont
    closeBtn.Parent = notifFrame
    
    notifFrame.Size = UDim2.new(1, 0, 0, 65)
    
    local slideIn = TweenService:Create(notifContainer, TweenInfo.new(0.35, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        Position = UDim2.new(1, -340, 1, -210)
    })
    slideIn:Play()
    
    local pulseUp = TweenService:Create(glowFrame, TweenInfo.new(0.4, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
        BackgroundTransparency = 0.85
    })
    local pulseDown = TweenService:Create(glowFrame, TweenInfo.new(0.4, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
        BackgroundTransparency = 0.92
    })
    
    local pulseLoop = true
    task.spawn(function()
        while pulseLoop and notifFrame.Parent do
            pulseUp:Play()
            pulseUp.Completed:Wait()
            pulseDown:Play()
            pulseDown.Completed:Wait()
        end
    end)
    
    closeBtn.MouseButton1Click:Connect(function()
        pulseLoop = false
        local slideOut = TweenService:Create(notifContainer, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
            Position = UDim2.new(1, -340, 1, -190)
        })
        slideOut:Play()
        task.wait(0.25)
        notifContainer:Destroy()
    end)
    
    task.delay(4.5, function()
        if notifContainer and notifContainer.Parent then
            pulseLoop = false
            local slideOut = TweenService:Create(notifContainer, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
                Position = UDim2.new(1, -340, 1, -190)
            })
            slideOut:Play()
            task.wait(0.25)
            notifContainer:Destroy()
        end
    end)
end

local function checkRoundStart()
    updateRoleVariables()
    
    local currentTime = tick()
    if currentTime - lastNotificationTime < 3 then return end
    
    local hasMurderer = currentMurderer ~= nil
    local hasSheriff = currentSheriff ~= nil
    
    if hasMurderer or hasSheriff then
        if not roundActive then
            roundActive = true
            if currentMurderer then
                createNotification("MURDERER", currentMurderer.Name, Color3.fromRGB(255, 50, 50))
            end
            if currentSheriff then
                createNotification("SHERIFF", currentSheriff.Name, Color3.fromRGB(50, 120, 255))
            end
            lastNotificationTime = currentTime
        end
    else
        roundActive = false
    end
end

-- ============================================
-- ANIMATED FOV CIRCLE
-- ============================================
local function createFOVCircle()
    if fovCircle then fovCircle:Destroy() end
    
    fovCircle = Instance.new("Frame")
    fovCircle.Size = UDim2.new(0, fovRadius * 2, 0, fovRadius * 2)
    fovCircle.Position = UDim2.new(0.5, -fovRadius, 0.5, -fovRadius)
    fovCircle.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
    fovCircle.BackgroundTransparency = 0.92
    fovCircle.BorderSizePixel = 0
    fovCircle.Visible = showFOVCircle
    fovCircle.Parent = screenGui
    
    local circleCorner = Instance.new("UICorner")
    circleCorner.CornerRadius = UDim.new(1, 0)
    circleCorner.Parent = fovCircle
    
    local circleStroke = Instance.new("UIStroke")
    circleStroke.Color = Color3.fromRGB(255, 50, 50)
    circleStroke.Thickness = 2
    circleStroke.Transparency = 0.6
    circleStroke.Parent = fovCircle
    
    local pulseUp = TweenService:Create(fovCircle, TweenInfo.new(0.6, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
        BackgroundTransparency = 0.88,
        Size = UDim2.new(0, (fovRadius * 2) + 8, 0, (fovRadius * 2) + 8),
        Position = UDim2.new(0.5, -(fovRadius + 4), 0.5, -(fovRadius + 4))
    })
    
    local pulseDown = TweenService:Create(fovCircle, TweenInfo.new(0.6, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
        BackgroundTransparency = 0.92,
        Size = UDim2.new(0, fovRadius * 2, 0, fovRadius * 2),
        Position = UDim2.new(0.5, -fovRadius, 0.5, -fovRadius)
    })
    
    if circleAnimation then circleAnimation:Disconnect() end
    circleAnimation = RunService.RenderStepped:Connect(function()
        if showFOVCircle and fovCircle and fovCircle.Visible then
            pulseUp:Play()
            pulseUp.Completed:Wait()
            pulseDown:Play()
            pulseDown.Completed:Wait()
        end
    end)
end

local function toggleFOVCircle()
    showFOVCircle = not showFOVCircle
    if fovCircle then
        fovCircle.Visible = showFOVCircle
    else
        createFOVCircle()
    end
end

-- ============================================
-- ROLE-BASED ESP (UPDATES EVERY 0.1 SECONDS)
-- ============================================

local function createESPForPlayer(player)
    if not player or player == plr then return end
    
    if espObjects[player] then
        local oldHighlight = espObjects[player]
        if oldHighlight and oldHighlight.Parent then
            oldHighlight:Destroy()
        end
        espObjects[player] = nil
    end
    
    local character = player.Character
    if not character then return end
    
    local highlight = Instance.new("Highlight")
    highlight.Name = "YagamiESP"
    highlight.Parent = character
    highlight.FillTransparency = 0.6
    highlight.OutlineTransparency = 0.2
    highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    
    local role = getPlayerRole(player)
    highlight.FillColor = ROLE_COLORS[role] or ROLE_COLORS.Innocent
    highlight.OutlineColor = highlight.FillColor
    
    espObjects[player] = highlight
end

local function removeESPForPlayer(player)
    if espObjects[player] then
        local highlight = espObjects[player]
        if highlight and highlight.Parent then
            highlight:Destroy()
        end
        espObjects[player] = nil
    end
end

local function updateAllESP()
    if not espEnabled then return end
    
    -- Update every 0.1 seconds
    local currentTime = tick()
    if currentTime - lastESPUpdate < 0.1 then return end
    lastESPUpdate = currentTime
    
    -- Get current players
    local currentPlayers = {}
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= plr then
            currentPlayers[player] = true
        end
    end
    
    -- Remove ESP for players that no longer exist
    for player, _ in pairs(espObjects) do
        if not currentPlayers[player] or not player.Character then
            removeESPForPlayer(player)
        end
    end
    
    -- Create/Update ESP for current players
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= plr then
            if espObjects[player] then
                local highlight = espObjects[player]
                local currentRole = getPlayerRole(player)
                local expectedColor = ROLE_COLORS[currentRole] or ROLE_COLORS.Innocent
                
                if highlight.FillColor ~= expectedColor then
                    highlight.FillColor = expectedColor
                    highlight.OutlineColor = expectedColor
                end
            else
                if player.Character then
                    createESPForPlayer(player)
                end
            end
        end
    end
end

local espUpdateConnection = nil

local function startESP()
    if espUpdateConnection then espUpdateConnection:Disconnect() end
    
    updateAllESP()
    
    -- Update every frame for 0.1 second accuracy
    espUpdateConnection = RunService.RenderStepped:Connect(function()
        if espEnabled then
            updateAllESP()
        end
    end)
    
    local function onPlayerAdded(player)
        if espEnabled and player ~= plr then
            task.wait(0.3)
            if player.Character then
                createESPForPlayer(player)
            end
        end
    end
    
    espEnabled.playerAddedConnection = Players.PlayerAdded:Connect(onPlayerAdded)
end

local function stopESP()
    if espUpdateConnection then
        espUpdateConnection:Disconnect()
        espUpdateConnection = nil
    end
    
    if espEnabled and espEnabled.playerAddedConnection then
        espEnabled.playerAddedConnection:Disconnect()
    end
    
    for player, highlight in pairs(espObjects) do
        if highlight and highlight.Parent then
            highlight:Destroy()
        end
    end
    espObjects = {}
end

-- ============================================
-- AUTO COLLECT COINS (FIXED)
-- ============================================
local autoCollectConnection = nil
local isCollecting = false
local noclipEnabled = false

local function setNoclip(state)
    noclipEnabled = state
    local character = plr.Character
    if character then
        for _, part in pairs(character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = not state
            end
        end
    end
end

local function flyToPosition(targetPos)
    local character = plr.Character
    if not character then return false end
    
    local humanoid = character:FindFirstChild("Humanoid")
    local rootPart = character:FindFirstChild("HumanoidRootPart")
    
    if not humanoid or not rootPart then return false end
    
    setNoclip(true)
    
    local originalSpeed = humanoid.WalkSpeed
    local originalPlatform = humanoid.PlatformStand
    
    humanoid.WalkSpeed = 25
    humanoid.PlatformStand = true
    
    local distance = (targetPos - rootPart.Position).Magnitude
    local duration = math.max(0.1, distance / 25)
    
    local flyTween = TweenService:Create(rootPart, TweenInfo.new(duration, Enum.EasingStyle.Linear), {
        Position = targetPos
    })
    flyTween:Play()
    flyTween.Completed:Wait()
    
    humanoid.PlatformStand = originalPlatform
    humanoid.WalkSpeed = originalSpeed
    setNoclip(false)
    
    return true
end

local function getCoins()
    local coins = {}
    local coinNames = {"Coin", "Money", "coin", "money", "Gem", "gem", "Token", "token"}
    
    for _, descendant in ipairs(Workspace:GetDescendants()) do
        if descendant:IsA("BasePart") and descendant.Parent then
            local isValid = false
            for _, name in pairs(coinNames) do
                if descendant.Name:lower():find(name:lower()) then
                    isValid = true
                    break
                end
            end
            
            if isValid and descendant.Parent and not descendant.Parent:IsA("Tool") and not descendant.Parent:IsA("Player") then
                if descendant:FindFirstChild("TouchInterest") then
                    table.insert(coins, descendant)
                end
            end
        end
    end
    return coins
end

local function collectClosestCoin()
    if isCollecting then return end
    if not autoCollectEnabled then return end
    
    local character = plr.Character
    if not character then return end
    
    local rootPart = character:FindFirstChild("HumanoidRootPart")
    if not rootPart then return end
    
    local coins = getCoins()
    if #coins == 0 then return end
    
    local closestCoin = nil
    local closestDist = math.huge
    
    for _, coin in ipairs(coins) do
        if coin and coin.Parent then
            local dist = (coin.Position - rootPart.Position).Magnitude
            if dist < closestDist then
                closestDist = dist
                closestCoin = coin
            end
        end
    end
    
    if closestCoin and closestCoin.Parent then
        isCollecting = true
        
        local success = flyToPosition(closestCoin.Position)
        
        if success and closestCoin.Parent then
            local touchInterest = closestCoin:FindFirstChild("TouchInterest")
            if touchInterest then
                pcall(function()
                    touchInterest:FireServer(rootPart)
                end)
            end
            
            -- Force touch by moving into the coin
            rootPart.CFrame = CFrame.new(closestCoin.Position)
        end
        
        isCollecting = false
    end
end

local function startAutoCollect()
    if autoCollectConnection then autoCollectConnection:Disconnect() end
    
    autoCollectConnection = RunService.Heartbeat:Connect(function()
        if autoCollectEnabled and not isCollecting then
            collectClosestCoin()
        end
    end)
end

local function stopAutoCollect()
    if autoCollectConnection then
        autoCollectConnection:Disconnect()
        autoCollectConnection = nil
    end
    isCollecting = false
    setNoclip(false)
end

-- ============================================
-- LOADING SCREEN
-- ============================================
local loadingFrame = Instance.new("Frame")
loadingFrame.Size = UDim2.new(0, 480, 0, 280)
loadingFrame.Position = UDim2.new(0.5, -240, 0.5, -140)
loadingFrame.BackgroundColor3 = Color3.fromRGB(8, 8, 16)
loadingFrame.BackgroundTransparency = 0.05
loadingFrame.BorderSizePixel = 0
loadingFrame.Parent = screenGui

local loadingCorner = Instance.new("UICorner")
loadingCorner.CornerRadius = UDim.new(0, 20)
loadingCorner.Parent = loadingFrame

local loadingGlow = Instance.new("Frame")
loadingGlow.Size = UDim2.new(1, 0, 1, 0)
loadingGlow.BackgroundColor3 = Color3.fromRGB(150, 50, 200)
loadingGlow.BackgroundTransparency = 0.96
loadingGlow.BorderSizePixel = 0
loadingGlow.Parent = loadingFrame

local loadingGlowCorner = Instance.new("UICorner")
loadingGlowCorner.CornerRadius = UDim.new(0, 20)
loadingGlowCorner.Parent = loadingGlow

local loadingTitle = Instance.new("TextLabel")
loadingTitle.Size = UDim2.new(1, 0, 0, 60)
loadingTitle.Position = UDim2.new(0, 0, 0, 20)
loadingTitle.BackgroundTransparency = 1
loadingTitle.Text = "YAGAMI HUB"
loadingTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
loadingTitle.TextSize = 34
loadingTitle.Font = retroFont
loadingTitle.Parent = loadingFrame

local loadingSub = Instance.new("TextLabel")
loadingSub.Size = UDim2.new(1, 0, 0, 25)
loadingSub.Position = UDim2.new(0, 0, 0, 80)
loadingSub.BackgroundTransparency = 1
loadingSub.Text = "MURDER MYSTERY 2"
loadingSub.TextColor3 = Color3.fromRGB(160, 160, 200)
loadingSub.TextSize = 14
loadingSub.Font = modernFont
loadingSub.Parent = loadingFrame

local progressBg = Instance.new("Frame")
progressBg.Size = UDim2.new(0.7, 0, 0, 6)
progressBg.Position = UDim2.new(0.15, 0, 0.55, 0)
progressBg.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
progressBg.BorderSizePixel = 0
progressBg.Parent = loadingFrame

local progressBgCorner = Instance.new("UICorner")
progressBgCorner.CornerRadius = UDim.new(1, 0)
progressBgCorner.Parent = progressBg

local progressFill = Instance.new("Frame")
progressFill.Size = UDim2.new(0, 0, 1, 0)
progressFill.BackgroundColor3 = Color3.fromRGB(150, 50, 200)
progressFill.BorderSizePixel = 0
progressFill.Parent = progressBg

local progressFillCorner = Instance.new("UICorner")
progressFillCorner.CornerRadius = UDim.new(1, 0)
progressFillCorner.Parent = progressFill

local percentText = Instance.new("TextLabel")
percentText.Size = UDim2.new(0.3, 0, 0, 35)
percentText.Position = UDim2.new(0.35, 0, 0.63, 0)
percentText.BackgroundTransparency = 1
percentText.Text = "0%"
percentText.TextColor3 = Color3.fromRGB(200, 200, 255)
percentText.TextSize = 22
percentText.Font = retroFont
percentText.Parent = loadingFrame

local subText = Instance.new("TextLabel")
subText.Size = UDim2.new(0.9, 0, 0, 25)
subText.Position = UDim2.new(0.05, 0, 0.78, 0)
subText.BackgroundTransparency = 1
subText.Text = "Initializing..."
subText.TextColor3 = Color3.fromRGB(150, 150, 180)
subText.TextSize = 13
subText.Font = modernFont
subText.Parent = loadingFrame

local accessText = Instance.new("TextLabel")
accessText.Size = UDim2.new(0.6, 0, 0, 35)
accessText.Position = UDim2.new(0.2, 0, 0.88, 0)
accessText.BackgroundTransparency = 1
accessText.Text = "ACCESS GRANTED"
accessText.TextColor3 = Color3.fromRGB(100, 255, 150)
accessText.TextSize = 16
accessText.Font = retroFont
accessText.Visible = false
accessText.Parent = loadingFrame

-- ============================================
-- MAIN GUI
-- ============================================
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 850, 0, 550)
mainFrame.Position = UDim2.new(0.5, -425, 0.5, -275)
mainFrame.BackgroundColor3 = Color3.fromRGB(6, 6, 14)
mainFrame.BackgroundTransparency = 0.1
mainFrame.BorderSizePixel = 0
mainFrame.Visible = false
mainFrame.Parent = screenGui
mainFrame.ClipsDescendants = true

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 20)
mainCorner.Parent = mainFrame

local mainGlow = Instance.new("Frame")
mainGlow.Size = UDim2.new(1, 0, 1, 0)
mainGlow.BackgroundColor3 = Color3.fromRGB(100, 50, 200)
mainGlow.BackgroundTransparency = 0.96
mainGlow.BorderSizePixel = 0
mainGlow.Parent = mainFrame

local mainGlowCorner = Instance.new("UICorner")
mainGlowCorner.CornerRadius = UDim.new(0, 20)
mainGlowCorner.Parent = mainGlow

-- Moon Rain VFX
local moonRainContainer = Instance.new("Frame")
moonRainContainer.Size = UDim2.new(1, 0, 1, 0)
moonRainContainer.BackgroundTransparency = 1
moonRainContainer.Parent = mainFrame

for i = 1, 35 do
    local moonLabel = Instance.new("TextLabel")
    moonLabel.Size = UDim2.new(0, 30, 0, 30)
    moonLabel.BackgroundTransparency = 1
    moonLabel.Text = "🌙"
    moonLabel.TextColor3 = Color3.fromRGB(180, 180, 255)
    moonLabel.TextSize = math.random(20, 35)
    moonLabel.TextTransparency = 0.5
    moonLabel.Font = retroFont
    moonLabel.Parent = moonRainContainer
    
    local startX = math.random(0, 850)
    local startY = math.random(-550, -50)
    moonLabel.Position = UDim2.new(0, startX, 0, startY)
    
    task.spawn(function()
        while moonLabel and moonLabel.Parent do
            local y = moonLabel.Position.Y.Offset + math.random(35, 65)
            if y > 600 then
                y = -50
                moonLabel.Position = UDim2.new(0, math.random(0, 850), 0, y)
            else
                moonLabel.Position = UDim2.new(0, moonLabel.Position.X.Offset + math.sin(y * 0.01) * 0.5, 0, y)
            end
            task.wait(0.05)
        end
    end)
end

-- Title Bar
local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(1, 0, 0, 50)
titleBar.Position = UDim2.new(0, 0, 0, 0)
titleBar.BackgroundColor3 = Color3.fromRGB(12, 12, 22)
titleBar.BackgroundTransparency = 0.3
titleBar.BorderSizePixel = 0
titleBar.Parent = mainFrame

local titleBarCorner = Instance.new("UICorner")
titleBarCorner.CornerRadius = UDim.new(0, 20)
titleBarCorner.Parent = titleBar

local titleText = Instance.new("TextLabel")
titleText.Size = UDim2.new(0.5, 0, 1, 0)
titleText.Position = UDim2.new(0.05, 0, 0, 0)
titleText.BackgroundTransparency = 1
titleText.Text = "YAGAMI HUB"
titleText.TextColor3 = Color3.fromRGB(255, 255, 255)
titleText.TextSize = 24
titleText.TextXAlignment = Enum.TextXAlignment.Left
titleText.Font = retroFont
titleText.Parent = titleBar

local titleSubText = Instance.new("TextLabel")
titleSubText.Size = UDim2.new(0.5, 0, 0.5, 0)
titleSubText.Position = UDim2.new(0.05, 0, 0.5, 0)
titleSubText.BackgroundTransparency = 1
titleSubText.Text = "MM2 EDITION"
titleSubText.TextColor3 = Color3.fromRGB(150, 150, 200)
titleSubText.TextSize = 13
titleSubText.TextXAlignment = Enum.TextXAlignment.Left
titleSubText.Font = modernFont
titleSubText.Parent = titleBar

local toggleBtn = Instance.new("TextButton")
toggleBtn.Size = UDim2.new(0, 42, 0, 42)
toggleBtn.Position = UDim2.new(1, -52, 0, 4)
toggleBtn.BackgroundColor3 = Color3.fromRGB(50, 35, 35)
toggleBtn.BackgroundTransparency = 0.3
toggleBtn.BorderSizePixel = 0
toggleBtn.Text = "✕"
toggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
toggleBtn.TextSize = 20
toggleBtn.Font = retroFont
toggleBtn.Parent = titleBar

local toggleCorner = Instance.new("UICorner")
toggleCorner.CornerRadius = UDim.new(0, 10)
toggleCorner.Parent = toggleBtn

-- Side Navigation
local navFrame = Instance.new("Frame")
navFrame.Size = UDim2.new(0, 160, 1, -60)
navFrame.Position = UDim2.new(1, -170, 0, 55)
navFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 18)
navFrame.BackgroundTransparency = 0.4
navFrame.BorderSizePixel = 0
navFrame.Parent = mainFrame

local navCorner = Instance.new("UICorner")
navCorner.CornerRadius = UDim.new(0, 12)
navCorner.Parent = navFrame

local contentFrame = Instance.new("Frame")
contentFrame.Size = UDim2.new(1, -190, 1, -60)
contentFrame.Position = UDim2.new(0, 10, 0, 55)
contentFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 18)
contentFrame.BackgroundTransparency = 0.4
contentFrame.BorderSizePixel = 0
contentFrame.Parent = mainFrame

local contentCorner = Instance.new("UICorner")
contentCorner.CornerRadius = UDim.new(0, 12)
contentCorner.Parent = contentFrame

-- Section Buttons
local sections = {"MAIN", "COMBAT", "AUTO-FARM", "VISUALS", "SETTINGS"}
local sectionButtons = {}
local activeSection = nil

local function createSectionContent(sectionName)
    for _, child in pairs(contentFrame:GetChildren()) do
        if child.Name ~= "UICorner" and child.Name ~= "UIListLayout" then
            child:Destroy()
        end
    end
    
    local scrollFrame = Instance.new("ScrollingFrame")
    scrollFrame.Size = UDim2.new(1, 0, 1, -50)
    scrollFrame.Position = UDim2.new(0, 0, 0, 50)
    scrollFrame.BackgroundTransparency = 1
    scrollFrame.BorderSizePixel = 0
    scrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
    scrollFrame.ScrollBarThickness = 4
    scrollFrame.ScrollBarImageColor3 = Color3.fromRGB(100, 50, 200)
    scrollFrame.Parent = contentFrame
    
    local uiList = Instance.new("UIListLayout")
    uiList.Padding = UDim.new(0, 12)
    uiList.SortOrder = Enum.SortOrder.LayoutOrder
    uiList.Parent = scrollFrame
    
    local sectionTitle = Instance.new("TextLabel")
    sectionTitle.Size = UDim2.new(1, 0, 0, 45)
    sectionTitle.BackgroundTransparency = 1
    sectionTitle.Text = sectionName
    sectionTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
    sectionTitle.TextSize = 26
    sectionTitle.Font = retroFont
    sectionTitle.TextXAlignment = Enum.TextXAlignment.Center
    sectionTitle.Parent = scrollFrame
    
    if sectionName == "MAIN" then
        local roleCard = Instance.new("Frame")
        roleCard.Size = UDim2.new(1, -20, 0, 100)
        roleCard.BackgroundColor3 = Color3.fromRGB(18, 18, 30)
        roleCard.BackgroundTransparency = 0.4
        roleCard.BorderSizePixel = 0
        roleCard.Parent = scrollFrame
        
        local roleCardCorner = Instance.new("UICorner")
        roleCardCorner.CornerRadius = UDim.new(0, 12)
        roleCardCorner.Parent = roleCard
        
        local roleLabel = Instance.new("TextLabel")
        roleLabel.Size = UDim2.new(1, 0, 0, 25)
        roleLabel.Position = UDim2.new(0, 0, 0, 10)
        roleLabel.BackgroundTransparency = 1
        roleLabel.Text = "YOUR ROLE"
        roleLabel.TextColor3 = Color3.fromRGB(180, 180, 220)
        roleLabel.TextSize = 14
        roleLabel.Font = modernFont
        roleLabel.TextXAlignment = Enum.TextXAlignment.Center
        roleLabel.Parent = roleCard
        
        local roleValue = Instance.new("TextLabel")
        roleValue.Size = UDim2.new(1, 0, 0, 40)
        roleValue.Position = UDim2.new(0, 0, 0, 35)
        roleValue.BackgroundTransparency = 1
        roleValue.Text = currentRole:upper()
        roleValue.TextColor3 = currentRole == "Murderer" and Color3.fromRGB(255, 80, 80) or (currentRole == "Sheriff" and Color3.fromRGB(80, 150, 255) or Color3.fromRGB(80, 255, 80))
        roleValue.TextSize = 30
        roleValue.Font = retroFont
        roleValue.TextXAlignment = Enum.TextXAlignment.Center
        roleValue.Parent = roleCard
        
        -- Fling Buttons
        local flingMurdererBtn = Instance.new("TextButton")
        flingMurdererBtn.Size = UDim2.new(0.48, 0, 0, 45)
        flingMurdererBtn.Position = UDim2.new(0, 0, 0, 120)
        flingMurdererBtn.BackgroundColor3 = Color3.fromRGB(140, 30, 30)
        flingMurdererBtn.BorderSizePixel = 0
        flingMurdererBtn.Text = "🔪 FLING MURDERER"
        flingMurdererBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        flingMurdererBtn.TextSize = 13
        flingMurdererBtn.Font = retroFont
        flingMurdererBtn.Parent = scrollFrame
        
        local flingMurdererCorner = Instance.new("UICorner")
        flingMurdererCorner.CornerRadius = UDim.new(0, 8)
        flingMurdererCorner.Parent = flingMurdererBtn
        
        local flingSheriffBtn = Instance.new("TextButton")
        flingSheriffBtn.Size = UDim2.new(0.48, 0, 0, 45)
        flingSheriffBtn.Position = UDim2.new(0.52, 0, 0, 120)
        flingSheriffBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 140)
        flingSheriffBtn.BorderSizePixel = 0
        flingSheriffBtn.Text = "🔫 FLING SHERIFF"
        flingSheriffBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        flingSheriffBtn.TextSize = 13
        flingSheriffBtn.Font = retroFont
        flingSheriffBtn.Parent = scrollFrame
        
        local flingSheriffCorner = Instance.new("UICorner")
        flingSheriffCorner.CornerRadius = UDim.new(0, 8)
        flingSheriffCorner.Parent = flingSheriffBtn
        
        flingMurdererBtn.MouseButton1Click:Connect(function()
            local murderer = getMurderer()
            if murderer then
                flingPlayer(murderer)
            end
        end)
        
        flingSheriffBtn.MouseButton1Click:Connect(function()
            local sheriff = getSheriff()
            if sheriff then
                flingPlayer(sheriff)
            end
        end)
        
        task.spawn(function()
            while scrollFrame and scrollFrame.Parent do
                updateSelfRole()
                updateRoleVariables()
                roleValue.Text = currentRole:upper()
                roleValue.TextColor3 = currentRole == "Murderer" and Color3.fromRGB(255, 80, 80) or (currentRole == "Sheriff" and Color3.fromRGB(80, 150, 255) or Color3.fromRGB(80, 255, 80))
                task.wait(0.5)
            end
        end)
        
    elseif sectionName == "COMBAT" then
        -- Aimbot Toggle
        local aimbotCard = Instance.new("Frame")
        aimbotCard.Size = UDim2.new(1, -20, 0, 55)
        aimbotCard.BackgroundColor3 = Color3.fromRGB(18, 18, 30)
        aimbotCard.BackgroundTransparency = 0.4
        aimbotCard.BorderSizePixel = 0
        aimbotCard.Parent = scrollFrame
        
        local aimbotCardCorner = Instance.new("UICorner")
        aimbotCardCorner.CornerRadius = UDim.new(0, 10)
        aimbotCardCorner.Parent = aimbotCard
        
        local aimbotLabel = Instance.new("TextLabel")
        aimbotLabel.Size = UDim2.new(0.6, 0, 1, 0)
        aimbotLabel.Position = UDim2.new(0.05, 0, 0, 0)
        aimbotLabel.BackgroundTransparency = 1
        aimbotLabel.Text = "🎯 AIMBOT (Murderer Only)"
        aimbotLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        aimbotLabel.TextSize = 15
        aimbotLabel.Font = retroFont
        aimbotLabel.TextXAlignment = Enum.TextXAlignment.Left
        aimbotLabel.Parent = aimbotCard
        
        local aimbotToggle = Instance.new("TextButton")
        aimbotToggle.Size = UDim2.new(0, 75, 0, 38)
        aimbotToggle.Position = UDim2.new(0.75, 0, 0.5, -19)
        aimbotToggle.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
        aimbotToggle.BorderSizePixel = 0
        aimbotToggle.Text = "OFF"
        aimbotToggle.TextColor3 = Color3.fromRGB(255, 255, 255)
        aimbotToggle.TextSize = 15
        aimbotToggle.Font = retroFont
        aimbotToggle.Parent = aimbotCard
        
        local aimbotToggleCorner = Instance.new("UICorner")
        aimbotToggleCorner.CornerRadius = UDim.new(0, 8)
        aimbotToggleCorner.Parent = aimbotToggle
        
        -- Silent Aim Toggle
        local silentCard = Instance.new("Frame")
        silentCard.Size = UDim2.new(1, -20, 0, 55)
        silentCard.Position = UDim2.new(0, 0, 0, 70)
        silentCard.BackgroundColor3 = Color3.fromRGB(18, 18, 30)
        silentCard.BackgroundTransparency = 0.4
        silentCard.BorderSizePixel = 0
        silentCard.Parent = scrollFrame
        
        local silentCardCorner = Instance.new("UICorner")
        silentCardCorner.CornerRadius = UDim.new(0, 10)
        silentCardCorner.Parent = silentCard
        
        local silentLabel = Instance.new("TextLabel")
        silentLabel.Size = UDim2.new(0.6, 0, 1, 0)
        silentLabel.Position = UDim2.new(0.05, 0, 0, 0)
        silentLabel.BackgroundTransparency = 1
        silentLabel.Text = "🔫 SILENT AIM (Universal)"
        silentLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        silentLabel.TextSize = 15
        silentLabel.Font = retroFont
        silentLabel.TextXAlignment = Enum.TextXAlignment.Left
        silentLabel.Parent = silentCard
        
        local silentToggle = Instance.new("TextButton")
        silentToggle.Size = UDim2.new(0, 75, 0, 38)
        silentToggle.Position = UDim2.new(0.75, 0, 0.5, -19)
        silentToggle.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
        silentToggle.BorderSizePixel = 0
        silentToggle.Text = "OFF"
        silentToggle.TextColor3 = Color3.fromRGB(255, 255, 255)
        silentToggle.TextSize = 15
        silentToggle.Font = retroFont
        silentToggle.Parent = silentCard
        
        local silentToggleCorner = Instance.new("UICorner")
        silentToggleCorner.CornerRadius = UDim.new(0, 8)
        silentToggleCorner.Parent = silentToggle
        
        -- FOV Circle Toggle
        local fovCard = Instance.new("Frame")
        fovCard.Size = UDim2.new(1, -20, 0, 55)
        fovCard.Position = UDim2.new(0, 0, 0, 140)
        fovCard.BackgroundColor3 = Color3.fromRGB(18, 18, 30)
        fovCard.BackgroundTransparency = 0.4
        fovCard.BorderSizePixel = 0
        fovCard.Parent = scrollFrame
        
        local fovCardCorner = Instance.new("UICorner")
        fovCardCorner.CornerRadius = UDim.new(0, 10)
        fovCardCorner.Parent = fovCard
        
        local fovLabel = Instance.new("TextLabel")
        fovLabel.Size = UDim2.new(0.6, 0, 1, 0)
        fovLabel.Position = UDim2.new(0.05, 0, 0, 0)
        fovLabel.BackgroundTransparency = 1
        fovLabel.Text = "🎯 SHOW FOV CIRCLE"
        fovLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        fovLabel.TextSize = 15
        fovLabel.Font = retroFont
        fovLabel.TextXAlignment = Enum.TextXAlignment.Left
        fovLabel.Parent = fovCard
        
        local fovToggle = Instance.new("TextButton")
        fovToggle.Size = UDim2.new(0, 75, 0, 38)
        fovToggle.Position = UDim2.new(0.75, 0, 0.5, -19)
        fovToggle.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
        fovToggle.BorderSizePixel = 0
        fovToggle.Text = "OFF"
        fovToggle.TextColor3 = Color3.fromRGB(255, 255, 255)
        fovToggle.TextSize = 15
        fovToggle.Font = retroFont
        fovToggle.Parent = fovCard
        
        local fovToggleCorner = Instance.new("UICorner")
        fovToggleCorner.CornerRadius = UDim.new(0, 8)
        fovToggleCorner.Parent = fovToggle
        
        aimbotToggle.MouseButton1Click:Connect(function()
            if aimbotEnabled then
                aimbotEnabled = false
                stopAimbot()
                aimbotToggle.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
                aimbotToggle.Text = "OFF"
            else
                aimbotEnabled = true
                startAimbot()
                aimbotToggle.BackgroundColor3 = Color3.fromRGB(100, 50, 200)
                aimbotToggle.Text = "ON"
            end
        end)
        
        silentToggle.MouseButton1Click:Connect(function()
            if silentAimEnabled then
                silentAimEnabled = false
                stopSilentAim()
                silentToggle.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
                silentToggle.Text = "OFF"
            else
                silentAimEnabled = true
                startSilentAim()
                silentToggle.BackgroundColor3 = Color3.fromRGB(100, 50, 200)
                silentToggle.Text = "ON"
            end
        end)
        
        fovToggle.MouseButton1Click:Connect(function()
            toggleFOVCircle()
            if showFOVCircle then
                fovToggle.BackgroundColor3 = Color3.fromRGB(100, 50, 200)
                fovToggle.Text = "ON"
            else
                fovToggle.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
                fovToggle.Text = "OFF"
            end
        end)
        
    elseif sectionName == "AUTO-FARM" then
        local farmCard = Instance.new("Frame")
        farmCard.Size = UDim2.new(1, -20, 0, 55)
        farmCard.BackgroundColor3 = Color3.fromRGB(18, 18, 30)
        farmCard.BackgroundTransparency = 0.4
        farmCard.BorderSizePixel = 0
        farmCard.Parent = scrollFrame
        
        local farmCardCorner = Instance.new("UICorner")
        farmCardCorner.CornerRadius = UDim.new(0, 10)
        farmCardCorner.Parent = farmCard
        
        local farmLabel = Instance.new("TextLabel")
        farmLabel.Size = UDim2.new(0.6, 0, 1, 0)
        farmLabel.Position = UDim2.new(0.05, 0, 0, 0)
        farmLabel.BackgroundTransparency = 1
        farmLabel.Text = "💰 AUTO COLLECT COINS"
        farmLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        farmLabel.TextSize = 15
        farmLabel.Font = retroFont
        farmLabel.TextXAlignment = Enum.TextXAlignment.Left
        farmLabel.Parent = farmCard
        
        local farmToggle = Instance.new("TextButton")
        farmToggle.Size = UDim2.new(0, 75, 0, 38)
        farmToggle.Position = UDim2.new(0.75, 0, 0.5, -19)
        farmToggle.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
        farmToggle.BorderSizePixel = 0
        farmToggle.Text = "OFF"
        farmToggle.TextColor3 = Color3.fromRGB(255, 255, 255)
        farmToggle.TextSize = 15
        farmToggle.Font = retroFont
        farmToggle.Parent = farmCard
        
        local farmToggleCorner = Instance.new("UICorner")
        farmToggleCorner.CornerRadius = UDim.new(0, 8)
        farmToggleCorner.Parent = farmToggle
        
        local farmInfo = Instance.new("TextLabel")
        farmInfo.Size = UDim2.new(1, -20, 0, 60)
        farmInfo.Position = UDim2.new(0, 10, 0, 75)
        farmInfo.BackgroundTransparency = 1
        farmInfo.TextColor3 = Color3.fromRGB(150, 150, 180)
        farmInfo.TextSize = 12
        farmInfo.Font = modernFont
        farmInfo.TextWrapped = true
        farmInfo.Text = "Flies through floors at speed 25 to collect coins.\nPrioritizes the closest coin first."
        farmInfo.Parent = scrollFrame
        
        farmToggle.MouseButton1Click:Connect(function()
            if autoCollectEnabled then
                autoCollectEnabled = false
                stopAutoCollect()
                farmToggle.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
                farmToggle.Text = "OFF"
            else
                autoCollectEnabled = true
                startAutoCollect()
                farmToggle.BackgroundColor3 = Color3.fromRGB(100, 50, 200)
                farmToggle.Text = "ON"
            end
        end)
        
    elseif sectionName == "VISUALS" then
        local espCard = Instance.new("Frame")
        espCard.Size = UDim2.new(1, -20, 0, 55)
        espCard.BackgroundColor3 = Color3.fromRGB(18, 18, 30)
        espCard.BackgroundTransparency = 0.4
        espCard.BorderSizePixel = 0
        espCard.Parent = scrollFrame
        
        local espCardCorner = Instance.new("UICorner")
        espCardCorner.CornerRadius = UDim.new(0, 10)
        espCardCorner.Parent = espCard
        
        local espLabel = Instance.new("TextLabel")
        espLabel.Size = UDim2.new(0.6, 0, 1, 0)
        espLabel.Position = UDim2.new(0.05, 0, 0, 0)
        espLabel.BackgroundTransparency = 1
        espLabel.Text = "👁️ ROLE-BASED ESP"
        espLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        espLabel.TextSize = 15
        espLabel.Font = retroFont
        espLabel.TextXAlignment = Enum.TextXAlignment.Left
        espLabel.Parent = espCard
        
        local espToggle = Instance.new("TextButton")
        espToggle.Size = UDim2.new(0, 75, 0, 38)
        espToggle.Position = UDim2.new(0.75, 0, 0.5, -19)
        espToggle.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
        espToggle.BorderSizePixel = 0
        espToggle.Text = "OFF"
        espToggle.TextColor3 = Color3.fromRGB(255, 255, 255)
        espToggle.TextSize = 15
        espToggle.Font = retroFont
        espToggle.Parent = espCard
        
        local espToggleCorner = Instance.new("UICorner")
        espToggleCorner.CornerRadius = UDim.new(0, 8)
        espToggleCorner.Parent = espToggle
        
        local espInfo = Instance.new("TextLabel")
        espInfo.Size = UDim2.new(1, -20, 0, 80)
        espInfo.Position = UDim2.new(0, 10, 0, 75)
        espInfo.BackgroundTransparency = 1
        espInfo.TextColor3 = Color3.fromRGB(150, 150, 180)
        espInfo.TextSize = 12
        espInfo.Font = modernFont
        espInfo.TextWrapped = true
        espInfo.Text = "🔴 MURDERER = Red\n🔵 SHERIFF = Blue\n🟢 INNOCENT = Green\n\nUpdates every 0.1 seconds for accuracy."
        espInfo.Parent = scrollFrame
        
        espToggle.MouseButton1Click:Connect(function()
            if espEnabled then
                espEnabled = false
                stopESP()
                espToggle.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
                espToggle.Text = "OFF"
            else
                espEnabled = true
                startESP()
                espToggle.BackgroundColor3 = Color3.fromRGB(100, 50, 200)
                espToggle.Text = "ON"
            end
        end)
        
    elseif sectionName == "SETTINGS" then
        local settingsCard = Instance.new("Frame")
        settingsCard.Size = UDim2.new(1, -20, 0, 120)
        settingsCard.BackgroundColor3 = Color3.fromRGB(18, 18, 30)
        settingsCard.BackgroundTransparency = 0.4
        settingsCard.BorderSizePixel = 0
        settingsCard.Parent = scrollFrame
        
        local settingsCardCorner = Instance.new("UICorner")
        settingsCardCorner.CornerRadius = UDim.new(0, 10)
        settingsCardCorner.Parent = settingsCard
        
        local settingsText = Instance.new("TextLabel")
        settingsText.Size = UDim2.new(1, 0, 1, 0)
        settingsText.BackgroundTransparency = 1
        settingsText.TextColor3 = Color3.fromRGB(200, 200, 220)
        settingsText.TextSize = 13
        settingsText.Font = modernFont
        settingsText.TextWrapped = true
        settingsText.Text = "⚙️ SETTINGS\n\n• Press R to toggle GUI\n• Drag title bar to move\n• GUI is fully customizable\n• Made for Murder Mystery 2"
        settingsText.Parent = settingsCard
    end
    
    local function updateCanvas()
        local totalHeight = 20
        for _, child in pairs(scrollFrame:GetChildren()) do
            if child:IsA("GuiObject") and child ~= scrollFrame and child.Name ~= "UIListLayout" then
                totalHeight = totalHeight + child.Size.Y.Offset + 12
            end
        end
        scrollFrame.CanvasSize = UDim2.new(0, 0, 0, totalHeight + 50)
    end
    
    uiList:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(updateCanvas)
    task.wait(0.1)
    updateCanvas()
end

for i, sectionName in ipairs(sections) do
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.85, 0, 0, 42)
    btn.Position = UDim2.new(0.075, 0, 0, (i-1) * 52 + 15)
    btn.BackgroundColor3 = Color3.fromRGB(22, 22, 40)
    btn.BackgroundTransparency = 0.3
    btn.BorderSizePixel = 0
    btn.Text = sectionName
    btn.TextColor3 = Color3.fromRGB(220, 220, 255)
    btn.TextSize = 14
    btn.Font = retroFont
    btn.Parent = navFrame
    
    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 8)
    btnCorner.Parent = btn
    
    btn.MouseEnter:Connect(function()
        if activeSection ~= btn then
            btn.BackgroundColor3 = Color3.fromRGB(45, 45, 70)
        end
    end)
    
    btn.MouseLeave:Connect(function()
        if activeSection ~= btn then
            btn.BackgroundColor3 = Color3.fromRGB(22, 22, 40)
        end
    end)
    
    btn.MouseButton1Click:Connect(function()
        for _, b in pairs(sectionButtons) do
            b.BackgroundColor3 = Color3.fromRGB(22, 22, 40)
            b.TextColor3 = Color3.fromRGB(220, 220, 255)
        end
        btn.BackgroundColor3 = Color3.fromRGB(100, 50, 200)
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        activeSection = btn
        createSectionContent(sectionName)
    end)
    
    sectionButtons[sectionName] = btn
end

-- ============================================
-- DRAG FUNCTIONALITY
-- ============================================
local dragging = false
local dragStart = nil
local frameStart = nil

titleBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        frameStart = mainFrame.Position
    end
end)

UserInput.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        mainFrame.Position = UDim2.new(frameStart.X.Scale, frameStart.X.Offset + delta.X, frameStart.Y.Scale, frameStart.Y.Offset + delta.Y)
    end
end)

UserInput.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = false
    end
end)

-- ============================================
-- TOGGLE GUI
-- ============================================
local isMinimized = false

local function toggleGUI()
    if isMinimized then
        local expandTween = TweenService:Create(mainFrame, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            Size = UDim2.new(0, 850, 0, 550)
        })
        expandTween:Play()
        navFrame.Visible = true
        contentFrame.Visible = true
        moonRainContainer.Visible = true
        toggleBtn.Text = "✕"
        toggleBtn.BackgroundColor3 = Color3.fromRGB(50, 35, 35)
        isMinimized = false
    else
        local shrinkTween = TweenService:Create(mainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
            Size = UDim2.new(0, 200, 0, 50)
        })
        shrinkTween:Play()
        navFrame.Visible = false
        contentFrame.Visible = false
        moonRainContainer.Visible = false
        toggleBtn.Text = "⚡"
        toggleBtn.BackgroundColor3 = Color3.fromRGB(35, 50, 35)
        isMinimized = true
    end
end

toggleBtn.MouseButton1Click:Connect(toggleGUI)
toggleBtn.TouchTap:Connect(toggleGUI)

-- ============================================
-- LOADING SEQUENCE
-- ============================================
local loadingMessages = {
    "Initializing Yagami Hub...",
    "Loading MM2 assets...",
    "Scanning inventory system...",
    "Setting up UI...",
    "Loading ESP modules...",
    "Almost ready...",
    "Access granted..."
}

local currentPercent = 0
local messageIndex = 1

local function updateLoading()
    if currentPercent >= 100 then
        accessText.Visible = true
        
        local flickerCount = 0
        local function flicker()
            if flickerCount < 8 then
                accessText.TextTransparency = flickerCount % 2 == 0 and 0 or 0.8
                flickerCount = flickerCount + 1
                task.wait(0.07)
                flicker()
            else
                accessText.TextTransparency = 0
                task.wait(0.3)
                transitionToMain()
            end
        end
        flicker()
        return
    end
    
    currentPercent = currentPercent + 1
    progressFill.Size = UDim2.new(currentPercent / 100, 0, 1, 0)
    percentText.Text = currentPercent .. "%"
    
    local newMessageIndex = math.floor(currentPercent / 14) + 1
    if newMessageIndex <= #loadingMessages and newMessageIndex ~= messageIndex then
        messageIndex = newMessageIndex
        subText.Text = loadingMessages[messageIndex]
    end
    
    task.wait(0.02)
    updateLoading()
end

function transitionToMain()
    local hideTween = TweenService:Create(loadingFrame, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.In), {
        Size = UDim2.new(0, 0, 0, 0)
    })
    hideTween:Play()
    
    task.wait(0.3)
    loadingFrame.Visible = false
    
    mainFrame.Visible = true
    mainFrame.Size = UDim2.new(0, 0, 0, 0)
    
    local showTween = TweenService:Create(mainFrame, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        Size = UDim2.new(0, 850, 0, 550)
    })
    showTween:Play()
    
    task.wait(0.2)
    if sectionButtons["MAIN"] then
        sectionButtons["MAIN"].MouseButton1Click:Fire()
    end
    
    print("YAGAMI HUB - MM2 Ultimate Edition Loaded!")
end

updateLoading()

-- ============================================
-- AUTOMATIC ROUND CHECK LOOP
-- ============================================
task.spawn(function()
    while true do
        task.wait(2)
        checkRoundStart()
    end
end)

-- ============================================
-- RE-OPEN HOTKEY (Press R)
-- ============================================
UserInput.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == Enum.KeyCode.R then
        if isMinimized then
            toggleGUI()
        elseif not mainFrame.Visible then
            screenGui.Enabled = true
            mainFrame.Visible = true
            mainFrame.Size = UDim2.new(0, 850, 0, 550)
            navFrame.Visible = true
            contentFrame.Visible = true
            moonRainContainer.Visible = true
            toggleBtn.Text = "✕"
            isMinimized = false
            if sectionButtons["MAIN"] then
                sectionButtons["MAIN"].MouseButton1Click:Fire()
            end
        end
    end
end)

-- Cleanup on character added
plr.CharacterAdded:Connect(function()
    task.wait(1)
    updateSelfRole()
    if autoCollectEnabled then
        stopAutoCollect()
        startAutoCollect()
    end
    if aimbotEnabled then
        stopAimbot()
        startAimbot()
    end
    if silentAimEnabled then
        stopSilentAim()
        startSilentAim()
    end
end)

-- Initial role update
task.wait(2)
updateSelfRole()
updateRoleVariables()

print("YAGAMI HUB - MM2 Ultimate Ready! All features working: Aimbot, Silent Aim, Fling, Notifications, Auto Farm, ESP (0.1s)")
