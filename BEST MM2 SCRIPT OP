-- YAGAMI HUB - COMPLETE WORKING SCRIPT
-- Works on Delta, Synapse, Krnl, ScriptWare, Fluxus, Oxygen U

repeat wait() until game:IsLoaded()

local plr = game:GetService("Players").LocalPlayer
local UserInput = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")

-- Create GUI
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "YagamiHub"
screenGui.Parent = plr:WaitForChild("PlayerGui")
screenGui.ResetOnSpawn = false

-- ============================================
-- LOADING SCREEN (Mini Rectangle)
-- ============================================
local loadingFrame = Instance.new("Frame")
loadingFrame.Size = UDim2.new(0, 400, 0, 200)
loadingFrame.Position = UDim2.new(0.5, -200, 0.5, -100)
loadingFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 35)
loadingFrame.BackgroundTransparency = 0
loadingFrame.BorderSizePixel = 0
loadingFrame.Parent = screenGui

local loadingCorner = Instance.new("UICorner")
loadingCorner.CornerRadius = UDim.new(0, 12)
loadingCorner.Parent = loadingFrame

local loadingStroke = Instance.new("UIStroke")
loadingStroke.Color = Color3.fromRGB(0, 200, 255)
loadingStroke.Thickness = 2
loadingStroke.Transparency = 0.5
loadingStroke.Parent = loadingFrame

local loadingTitle = Instance.new("TextLabel")
loadingTitle.Size = UDim2.new(1, 0, 0, 40)
loadingTitle.Position = UDim2.new(0, 0, 0, 15)
loadingTitle.BackgroundTransparency = 1
loadingTitle.Text = "YAGAMI HUB"
loadingTitle.TextColor3 = Color3.fromRGB(0, 200, 255)
loadingTitle.TextSize = 24
loadingTitle.Font = Enum.Font.GothamBold
loadingTitle.Parent = loadingFrame

local progressBg = Instance.new("Frame")
progressBg.Size = UDim2.new(0.8, 0, 0, 8)
progressBg.Position = UDim2.new(0.1, 0, 0.55, 0)
progressBg.BackgroundColor3 = Color3.fromRGB(40, 40, 60)
progressBg.BorderSizePixel = 0
progressBg.Parent = loadingFrame

local progressBgCorner = Instance.new("UICorner")
progressBgCorner.CornerRadius = UDim.new(1, 0)
progressBgCorner.Parent = progressBg

local progressFill = Instance.new("Frame")
progressFill.Size = UDim2.new(0, 0, 1, 0)
progressFill.BackgroundColor3 = Color3.fromRGB(0, 200, 255)
progressFill.BorderSizePixel = 0
progressFill.Parent = progressBg

local progressFillCorner = Instance.new("UICorner")
progressFillCorner.CornerRadius = UDim.new(1, 0)
progressFillCorner.Parent = progressFill

local percentText = Instance.new("TextLabel")
percentText.Size = UDim2.new(0.3, 0, 0, 30)
percentText.Position = UDim2.new(0.35, 0, 0.7, 0)
percentText.BackgroundTransparency = 1
percentText.Text = "0%"
percentText.TextColor3 = Color3.fromRGB(200, 200, 255)
percentText.TextSize = 18
percentText.Font = Enum.Font.Gotham
percentText.Parent = loadingFrame

local subText = Instance.new("TextLabel")
subText.Size = UDim2.new(0.9, 0, 0, 25)
subText.Position = UDim2.new(0.05, 0, 0.82, 0)
subText.BackgroundTransparency = 1
subText.Text = "Initializing..."
subText.TextColor3 = Color3.fromRGB(150, 150, 200)
subText.TextSize = 14
subText.Font = Enum.Font.Gotham
subText.Parent = loadingFrame

-- ============================================
-- MAIN GUI (Stretched Horizontally)
-- ============================================
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 800, 0, 500)
mainFrame.Position = UDim2.new(0.5, -400, 0.5, -250)
mainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 30)
mainFrame.BackgroundTransparency = 0
mainFrame.BorderSizePixel = 0
mainFrame.Visible = false
mainFrame.Parent = screenGui

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 12)
mainCorner.Parent = mainFrame

local mainStroke = Instance.new("UIStroke")
mainStroke.Color = Color3.fromRGB(0, 200, 255)
mainStroke.Thickness = 2
mainStroke.Transparency = 0.3
mainStroke.Parent = mainFrame

-- Title Bar
local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(1, 0, 0, 45)
titleBar.Position = UDim2.new(0, 0, 0, 0)
titleBar.BackgroundColor3 = Color3.fromRGB(25, 25, 45)
titleBar.BackgroundTransparency = 0
titleBar.BorderSizePixel = 0
titleBar.Parent = mainFrame

local titleBarCorner = Instance.new("UICorner")
titleBarCorner.CornerRadius = UDim.new(0, 12)
titleBarCorner.Parent = titleBar

-- Title text
local titleText = Instance.new("TextLabel")
titleText.Size = UDim2.new(0.5, 0, 1, 0)
titleText.Position = UDim2.new(0.05, 0, 0, 0)
titleText.BackgroundTransparency = 1
titleText.Text = "YAGAMI HUB"
titleText.TextColor3 = Color3.fromRGB(0, 200, 255)
titleText.TextSize = 22
titleText.TextXAlignment = Enum.TextXAlignment.Left
titleText.Font = Enum.Font.GothamBold
titleText.Parent = titleBar

-- Close/Minimize Button (changes between X and O)
local toggleBtn = Instance.new("TextButton")
toggleBtn.Size = UDim2.new(0, 40, 1, 0)
toggleBtn.Position = UDim2.new(1, -45, 0, 0)
toggleBtn.BackgroundColor3 = Color3.fromRGB(50, 35, 35)
toggleBtn.BackgroundTransparency = 0
toggleBtn.BorderSizePixel = 0
toggleBtn.Text = "X"
toggleBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
toggleBtn.TextSize = 20
toggleBtn.Font = Enum.Font.GothamBold
toggleBtn.Parent = titleBar

local toggleCorner = Instance.new("UICorner")
toggleCorner.CornerRadius = UDim.new(0, 8)
toggleCorner.Parent = toggleBtn

-- ============================================
-- SIDE NAVIGATION (5 Sections on the Right)
-- ============================================
local navFrame = Instance.new("Frame")
navFrame.Size = UDim2.new(0, 150, 1, -55)
navFrame.Position = UDim2.new(1, -160, 0, 50)
navFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 40)
navFrame.BackgroundTransparency = 0.2
navFrame.BorderSizePixel = 0
navFrame.Parent = mainFrame

local navCorner = Instance.new("UICorner")
navCorner.CornerRadius = UDim.new(0, 8)
navCorner.Parent = navFrame

-- Content display area (left side)
local contentFrame = Instance.new("Frame")
contentFrame.Size = UDim2.new(1, -170, 1, -55)
contentFrame.Position = UDim2.new(0, 10, 0, 50)
contentFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 40)
contentFrame.BackgroundTransparency = 0.3
contentFrame.BorderSizePixel = 0
contentFrame.Parent = mainFrame

local contentCorner = Instance.new("UICorner")
contentCorner.CornerRadius = UDim.new(0, 8)
contentCorner.Parent = contentFrame

-- Section buttons (5 sections)
local sections = {"Main", "Combat", "Auto-farm", "Visuals", "Settings"}
local sectionButtons = {}
local activeSection = nil

-- Function to create section content
local function createSectionContent(sectionName)
    -- Clear content frame
    for _, child in pairs(contentFrame:GetChildren()) do
        if child.Name ~= "UICorner" then
            child:Destroy()
        end
    end
    
    -- Create title for section
    local sectionTitle = Instance.new("TextLabel")
    sectionTitle.Size = UDim2.new(1, 0, 0, 40)
    sectionTitle.Position = UDim2.new(0, 0, 0, 10)
    sectionTitle.BackgroundTransparency = 1
    sectionTitle.Text = sectionName
    sectionTitle.TextColor3 = Color3.fromRGB(0, 200, 255)
    sectionTitle.TextSize = 24
    sectionTitle.Font = Enum.Font.GothamBold
    sectionTitle.TextXAlignment = Enum.TextXAlignment.Center
    sectionTitle.Parent = contentFrame
    
    -- Placeholder text
    local placeholderText = Instance.new("TextLabel")
    placeholderText.Size = UDim2.new(1, 0, 0, 30)
    placeholderText.Position = UDim2.new(0, 0, 0, 60)
    placeholderText.BackgroundTransparency = 1
    placeholderText.Text = "No functions added yet\nComing soon..."
    placeholderText.TextColor3 = Color3.fromRGB(150, 150, 200)
    placeholderText.TextSize = 16
    placeholderText.Font = Enum.Font.Gotham
    placeholderText.TextWrapped = true
    placeholderText.Parent = contentFrame
    
    -- Add specific placeholder based on section
    if sectionName == "Main" then
        placeholderText.Text = "Main Hub Functions\n\n- Player Info\n- Server Info\n- Rejoin\n- Server Hop"
    elseif sectionName == "Combat" then
        placeholderText.Text = "Combat Features\n\n- Aim Assist\n- Trigger Bot\n- Silent Aim\n- Anti-Aim"
    elseif sectionName == "Auto-farm" then
        placeholderText.Text = "Auto-Farm Features\n\n- Auto Farm\n- Auto Collect\n- Auto Quest\n- Auto Sell"
    elseif sectionName == "Visuals" then
        placeholderText.Text = "Visual Enhancements\n\n- ESP\n- Chams\n- Tracers\n- FOV Changer"
    elseif sectionName == "Settings" then
        placeholderText.Text = "Settings\n\n- GUI Settings\n- Keybinds\n- Save Config\n- Load Config"
    end
end

-- Create section buttons
for i, sectionName in ipairs(sections) do
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.9, 0, 0, 45)
    btn.Position = UDim2.new(0.05, 0, 0, (i-1) * 55 + 10)
    btn.BackgroundColor3 = Color3.fromRGB(30, 30, 55)
    btn.BackgroundTransparency = 0
    btn.BorderSizePixel = 0
    btn.Text = sectionName
    btn.TextColor3 = Color3.fromRGB(200, 200, 255)
    btn.TextSize = 16
    btn.Font = Enum.Font.GothamBold
    btn.Parent = navFrame
    
    local btnCorner = Instance.new("UICorner")
    btnCorner.CornerRadius = UDim.new(0, 6)
    btnCorner.Parent = btn
    
    -- Hover effects
    btn.MouseEnter:Connect(function()
        if activeSection ~= btn then
            btn.BackgroundColor3 = Color3.fromRGB(50, 50, 80)
        end
    end)
    
    btn.MouseLeave:Connect(function()
        if activeSection ~= btn then
            btn.BackgroundColor3 = Color3.fromRGB(30, 30, 55)
        end
    end)
    
    -- Click handler
    btn.MouseButton1Click:Connect(function()
        -- Reset all buttons
        for _, b in pairs(sectionButtons) do
            b.BackgroundColor3 = Color3.fromRGB(30, 30, 55)
            b.TextColor3 = Color3.fromRGB(200, 200, 255)
        end
        -- Highlight active button
        btn.BackgroundColor3 = Color3.fromRGB(0, 150, 200)
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        activeSection = btn
        -- Create content for this section
        createSectionContent(sectionName)
    end)
    
    sectionButtons[sectionName] = btn
end

-- Default select Main section
sectionButtons["Main"].MouseButton1Click:Fire()

-- ============================================
-- DRAG FUNCTIONALITY
-- ============================================
local dragging = false
local dragStartPos = nil
local frameStartPos = nil

titleBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStartPos = input.Position
        frameStartPos = mainFrame.Position
    end
end)

UserInput.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStartPos
        mainFrame.Position = UDim2.new(
            frameStartPos.X.Scale,
            frameStartPos.X.Offset + delta.X,
            frameStartPos.Y.Scale,
            frameStartPos.Y.Offset + delta.Y
        )
    end
end)

UserInput.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = false
    end
end)

-- ============================================
-- TOGGLE BUTTON FUNCTIONALITY (X and O)
-- ============================================
local isMinimized = false

local function toggleGUI()
    if isMinimized then
        -- EXPAND: Show full GUI
        local expandTween = TweenService:Create(mainFrame, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            Size = UDim2.new(0, 800, 0, 500)
        })
        expandTween:Play()
        
        -- Show navigation and content
        navFrame.Visible = true
        contentFrame.Visible = true
        
        -- Change button back to X
        toggleBtn.Text = "X"
        toggleBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
        toggleBtn.BackgroundColor3 = Color3.fromRGB(50, 35, 35)
        
        isMinimized = false
    else
        -- MINIMIZE: Shrink to title only
        local shrinkTween = TweenService:Create(mainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
            Size = UDim2.new(0, 200, 0, 45)
        })
        shrinkTween:Play()
        
        -- Hide navigation and content
        navFrame.Visible = false
        contentFrame.Visible = false
        
        -- Change button to O (open)
        toggleBtn.Text = "O"
        toggleBtn.TextColor3 = Color3.fromRGB(100, 255, 100)
        toggleBtn.BackgroundColor3 = Color3.fromRGB(35, 50, 35)
        
        isMinimized = true
    end
end

toggleBtn.MouseButton1Click:Connect(toggleGUI)
toggleBtn.TouchTap:Connect(toggleGUI)

-- ============================================
-- LOADING ANIMATION
-- ============================================
local loadingSteps = {
    "Initializing Yagami Hub...",
    "Loading assets...",
    "Connecting to server...",
    "Setting up UI...",
    "Loading modules...",
    "Almost ready...",
    "Welcome to Yagami Hub!"
}

local stepIndex = 1
for i = 1, 100 do
    wait(0.02)
    
    progressFill.Size = UDim2.new(i / 100, 0, 1, 0)
    percentText.Text = i .. "%"
    
    if i >= stepIndex * 14 and stepIndex <= #loadingSteps then
        subText.Text = loadingSteps[stepIndex]
        stepIndex = stepIndex + 1
    end
    
    local hue = 0.55 + (i / 100) * 0.2
    progressFill.BackgroundColor3 = Color3.fromHSV(hue, 1, 1)
end

-- ============================================
-- TRANSITION TO MAIN GUI
-- ============================================
local hideTween = TweenService:Create(loadingFrame, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.In), {
    Size = UDim2.new(0, 0, 0, 0)
})
hideTween:Play()

wait(0.3)
loadingFrame.Visible = false

mainFrame.Visible = true
mainFrame.Size = UDim2.new(0, 0, 0, 0)

local showTween = TweenService:Create(mainFrame, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
    Size = UDim2.new(0, 800, 0, 500)
})
showTween:Play()

-- Particle burst
local burst = Instance.new("ParticleEmitter")
burst.Texture = "rbxasset://textures/particles/sparkles_main.dds"
burst.Rate = 150
burst.Lifetime = NumberRange.new(0.5, 1)
burst.Speed = NumberRange.new(5, 15)
burst.Transparency = NumberSequence.new(1, 0)
burst.Parent = mainFrame

wait(0.5)
burst:Destroy()

print("YAGAMI HUB - Fully Loaded!")

-- ============================================
-- RE-OPEN HOTKEY (Press R)
-- ============================================
UserInput.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == Enum.KeyCode.R then
        if isMinimized then
            -- If minimized, expand it
            toggleGUI()
        elseif not mainFrame.Visible then
            -- If closed, reopen
            screenGui.Enabled = true
            mainFrame.Visible = true
            mainFrame.Size = UDim2.new(0, 800, 0, 500)
            navFrame.Visible = true
            contentFrame.Visible = true
            toggleBtn.Text = "X"
            isMinimized = false
        end
    end
end)
